# Landlord Portal — Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd frontend && npm ci"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd frontend && npx serve dist -s -l ${PORT:-8080}"
